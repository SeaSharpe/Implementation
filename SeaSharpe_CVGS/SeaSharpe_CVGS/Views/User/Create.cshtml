@model SeaSharpe_CVGS.Models.Member

@{
    ViewBag.Title = "Create";
}

<h2>Create</h2>

@using (Html.BeginForm()) 
{
    @Html.AntiForgeryToken()
    
    <div class="form-horizontal">
        <h4>Member</h4>
        <hr />
        @Html.ValidationSummary(true)

        <div class="form-group">
            @Html.LabelFor(model => model.IsEmailVerified, new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.EditorFor(model => model.IsEmailVerified)
                @Html.ValidationMessageFor(model => model.IsEmailVerified)
            </div>
        </div>

        <div class="form-group">
            @Html.LabelFor(model => model.IsEmailMarketingAllowed, new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.EditorFor(model => model.IsEmailMarketingAllowed)
                @Html.ValidationMessageFor(model => model.IsEmailMarketingAllowed)
            </div>
        </div>

        <div class="form-group">
            @Html.LabelFor(model => model.StripeID, new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.EditorFor(model => model.StripeID)
                @Html.ValidationMessageFor(model => model.StripeID)
            </div>
        </div>

        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Create" class="btn btn-default, greenButton" />
            </div>
        </div>
    </div>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>
