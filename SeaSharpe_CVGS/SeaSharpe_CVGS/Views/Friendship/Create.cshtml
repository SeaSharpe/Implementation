@model SeaSharpe_CVGS.Models.Friendship

@{
    ViewBag.Title = "Create";
}

<h2>Create</h2>

@using (Html.BeginForm()) 
{
    @Html.AntiForgeryToken()
    
    <div class="form-horizontal">
        <h4>Friendship</h4>
        <hr />
        @Html.ValidationSummary(true)

        <div class="form-group">
            @Html.LabelFor(model => model.FriendeeId, "FriendeeId", new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.DropDownList("FriendeeId", String.Empty)
                @Html.ValidationMessageFor(model => model.FriendeeId)
            </div>
        </div>

        <div class="form-group">
            @Html.LabelFor(model => model.FrienderId, "FrienderId", new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.DropDownList("FrienderId", String.Empty)
                @Html.ValidationMessageFor(model => model.FrienderId)
            </div>
        </div>

        <div class="form-group">
            @Html.LabelFor(model => model.IsFamilyMember, new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.EditorFor(model => model.IsFamilyMember)
                @Html.ValidationMessageFor(model => model.IsFamilyMember)
            </div>
        </div>

        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Create" class="btn btn-default" />
            </div>
        </div>
    </div>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>
