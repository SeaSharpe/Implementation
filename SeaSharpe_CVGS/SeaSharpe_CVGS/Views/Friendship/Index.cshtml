@model IEnumerable<SeaSharpe_CVGS.Models.Friendship>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Friendee.Id)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Friender.Id)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.IsFamilyMember)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Friendee.Id)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Friender.Id)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.IsFamilyMember)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
        </td>
    </tr>
}

</table>
